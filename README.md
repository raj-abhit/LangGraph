# LangGraph

A small collection of LangGraph notebook workflows for learning and experimentation.



## Quick start
1. Create and activate a Python virtual environment.
2. Install required packages used in the notebooks.
3. Add API keys in `.env` (for example `GOOGLE_API_KEY`, `GROQ_API_KEY`).
4. Open and run notebooks from top to bottom.
